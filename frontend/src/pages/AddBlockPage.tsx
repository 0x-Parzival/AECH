import React from 'react';

const AddBlockPage: React.FC = () => {
  return (
    <div>
      <h1>Add Block Page</h1>
      {/* Form or interface to add new blocks */}
    </div>
  );
};

export default AddBlockPage;
